# Шпаргалка по горячим клавишам nvim

Leader-клавиша (`<leader>`) — **пробел**. Комбинации вводятся по очереди
(пробел, отпустить, затем следующая клавиша), не одновременно.

## Telescope — нечёткий поиск

| Клавиша | Действие |
|---|---|
| `<leader>ff` | Найти файл по имени (нечёткий поиск) |
| `<leader>fg` | Grep — поиск по содержимому файлов во всём проекте |
| `<leader>fb` | Список открытых буферов |
| `<leader>fr` | Недавно открытые файлы |
| `<leader>fh` | Поиск по справке (`:help`) |
| `<leader>fd` | Диагностика LSP (ошибки/предупреждения) по проекту |
| `<leader>fs` | Символы (методы/классы/поля) текущего файла |
| `<leader>fS` | Символы по всему проекту (через LSP) |
| `<leader>fw` | Поиск слова под курсором по всему проекту |
| `<leader>fg` (в Visual-режиме) | Поиск выделенного текста по всему проекту |

Мнемоника: `f` — «find», вторая буква — что именно ищем (**f**iles, **g**rep,
**b**uffers, **r**ecent, **h**elp, **d**iagnostics, **s**ymbols, **w**ord).

### Искать по выделенному тексту

Выделите нужный текст в **Visual-режиме** (`v` + движение, или `viw` для слова
под курсором) и нажмите `<leader>fg` — откроется поиск по всему проекту с уже
готовым запросом (в отличие от обычного `<leader>fg` в Normal-режиме, где
запрос пустой и его нужно печатать самому).

### Внутри открытого окна Telescope

| Клавиша | Действие |
|---|---|
| `Ctrl-j` / `Ctrl-k` | Следующий / предыдущий результат |
| `Enter` | Открыть выбранное |
| `Esc` (дважды) | Закрыть Telescope |

## Файловый менеджер (nvim-tree)

| Клавиша | Действие |
|---|---|
| `<leader>e` | Открыть/закрыть дерево файлов |
| `<leader>o` | Перевести фокус на дерево файлов |

### Внутри дерева (курсор на файле/папке)

| Клавиша | Действие |
|---|---|
| `Enter` / `o` | Открыть файл или раскрыть папку |
| `a` | Создать файл/папку (со `/` на конце — папка) |
| `d` | Удалить |
| `r` | Переименовать |
| `x` / `p` | Вырезать / вставить |
| `c` / `p` | Скопировать / вставить |
| `R` | Обновить дерево |
| `H` | Показать/скрыть точечные файлы (dotfiles) |
| `q` | Закрыть дерево |

## Буферы (открытые файлы)

| Клавиша | Действие |
|---|---|
| `<leader>fb` | Список буферов (Telescope, см. выше) |
| `Shift-H` | Предыдущий буфер |
| `Shift-L` | Следующий буфер |
| `<leader>bd` | Закрыть текущий буфер, не закрывая окно |
| `:ls` | Обычный вимовый список буферов |

## LSP (Ruby/Python — работает, когда сервер подключён к буферу)

| Клавиша | Действие |
|---|---|
| `gd` | Перейти к определению (может открыть другой файл) |
| `gr` | Найти все использования (список внизу) |
| `K` | Документация/сигнатура под курсором |
| `<leader>rn` | Переименовать символ везде |
| `<leader>ca` | Code action (быстрые исправления) |
| `[d` / `]d` | Предыдущая / следующая ошибка или предупреждение |

## Автодополнение (во время ввода, insert-режим)

| Клавиша | Действие |
|---|---|
| `Tab` | Выбрать следующий вариант / развернуть сниппет |
| `Ctrl-j` / `Ctrl-k` | Следующий / предыдущий вариант |
| `Ctrl-Space` | Показать варианты вручную |
| `Enter` | Подтвердить выбор |
| `Ctrl-e` | Отменить автодополнение |

## Окна и общие клавиши

| Клавиша | Действие |
|---|---|
| `Ctrl-h/j/k/l` | Перейти в окно слева/снизу/сверху/справа |
| `<leader>w` | Сохранить файл |
| `<leader>q` | Закрыть окно |
| `Esc` | Убрать подсветку поиска |

## Запуск проекта

```sh
cd мой_проект
nvim .
```
Дерево при этом само не открывается (см. `<leader>e` выше) — открывайте вручную,
когда нужно осмотреть структуру проекта. Для навигации по коду удобнее сразу
`<leader>ff` / `<leader>fg`.

Для Ruby-проектов `gd`/`gr`/автодополнение работают только после `bundle install`
в проекте (нужен установленный `ruby_lsp`/`pyright`, ставятся автоматически через
Mason при первом запуске nvim).
